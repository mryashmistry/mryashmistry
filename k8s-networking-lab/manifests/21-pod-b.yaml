apiVersion: v1
kind: Pod
metadata:
  name: testpod-b
  namespace: netlab
spec:
  containers:
  - name: web
    image: hashicorp/http-echo
    args: ["-text=hello-from-B", "-listen=:8080"]
    ports:
    - containerPort: 8080

